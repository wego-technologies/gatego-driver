import 'package:flutter/material.dart';

class YardSelectionPage extends StatelessWidget {
  const YardSelectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Hello")),
    );
  }
}
